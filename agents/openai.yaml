interface:
  display_name: "直播竞品监看"
  short_description: "长时间采集直播竞品，输出中文目录和HTML看板"
  default_prompt: "Use $live-commerce-competitor-monitor to monitor a competitor live room and produce a Chinese delivery folder with recording, transcript, screenshots, structured data, speech analysis, traffic, engagement, order-signal data, and a local HTML dashboard when useful."

policy:
  allow_implicit_invocation: true
